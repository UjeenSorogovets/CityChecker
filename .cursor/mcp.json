{
  "mcpServers": {
    "citychecker": {
      "command": "${workspaceFolder}/tools/citychecker_mcp/.venv/Scripts/python.exe",
      "args": ["${workspaceFolder}/tools/citychecker_mcp/server.py"],
      "env": {
        "CITYCHECKER_BASE_URL": "http://localhost:8080",
        "CITYCHECKER_EMAIL": "mcp@citychecker.local",
        "CITYCHECKER_PASSWORD": "mcp-local-dev-password"
      }
    }
  }
}
